import { AddonStore } from "@storybook/addons";
import React from "react";

export default Empty = function (props) {
  return (
    <main className="appointment__add">
      <img className="appointment__add-button" src="images/add.png" alt="Add" />
    </main>
  );
};
