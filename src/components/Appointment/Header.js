import React from "react";

const Header = (props) => {
  <header className="appointment__time">
    <h4 className="text--semi-bold">12 pm</h4>
    <hr className="appointment__separator" />
  </header>;
};

export default Header;
